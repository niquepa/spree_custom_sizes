<% if @product.finishings.count > 0 %>
  <div class="row" id="finishings" data-hook>
    <div class="add-to-cart">
      <div class="col-md-10">
        <h4><%= Spree.t(:finishings_title) %>:</h4>
        <% @product.finishings.category.each do |finishing| %>
          <h1><%= finishing.presentation %></h1>
        <% end %>
      </div>
    </div>
  </div>
<% end %>